## AWS
+ AWS 설치 순서
``` 
  1. ec2 인스턴스 시작
    - t2.micro
    - 스토리지 크기 (8gb -> 30gb 변경)
    - Name 태그 추가 (인스턴스 이름 변경)
    - 보안 그룹 생성 (SSH : 22 내IP , HTTP: 80 0.0.0.0/0(기본) ,HTTPS 443 0.0.0.0/0(기본)
    - pem 키 저장
```
